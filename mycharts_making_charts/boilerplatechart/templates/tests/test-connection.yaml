apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "boilerplatechart.fullname" . }}-test-connection"
  labels:
    {{- include "boilerplatechart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "boilerplatechart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
