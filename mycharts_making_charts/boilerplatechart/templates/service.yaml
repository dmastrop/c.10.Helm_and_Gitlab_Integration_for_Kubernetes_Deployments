apiVersion: v1
kind: Service
metadata:
  name: {{ include "boilerplatechart.fullname" . }}
  labels:
    {{- include "boilerplatechart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "boilerplatechart.selectorLabels" . | nindent 4 }}
